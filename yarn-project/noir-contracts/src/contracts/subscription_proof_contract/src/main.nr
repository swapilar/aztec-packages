mod subscription_note;

contract Subcription {
    #[aztec(private)]
    fn subscribe( 
        token_contract: AztecAddress,
        amount: Field,
        nonce: Field,
    ) {
        // body goes here
    }
}
